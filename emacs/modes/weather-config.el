;;; weather-config.el --- Summary
;;; Commentary:
;;; Code:

(use-package forecast
  :config
  (setq forecast-city "Vancouver"
        forecast-country "Canada"
        forecast-language "en"))

;; sunshine
;; weather
;; weather-metno
